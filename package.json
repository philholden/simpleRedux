{
  "name": "simple-redux",
  "version": "0.0.1",
  "description": "Create Redux actions, reducer and constants for simple cases",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf lib",
    "test": "mocha test --compilers js:babel/register --recursive",
    "test:watch": "npm run test -- --watch",
    "build": "babel src --out-dir lib",
    "lint": "eslint src",
    "prepublish": "npm run lint && npm run test && npm run clean && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.8.23",
    "mocha": "^2.3.0",
    "babel-core": "^5.8.33",
    "babel-eslint": "^4.1.3",
    "eslint": "^0.23.0",
    "eslint-plugin-react": "^2.5.2",
    "node-libs-browser": "^0.5.2",
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "react": "^0.14.1",
    "react-dom": "^0.14.1",
    "react-redux": "^4.0.0",
    "redux": "^3.0.4"
  }
}
